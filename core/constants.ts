/**
 * The default attribution type and value used for metrics that are not
 * broken down by a specific attribution (e.g., total event counts).
 */
export const TOTAL_ATTRIBUTION = "_total";

/**
 * The string used in Redis keys to represent a null value.
 */
export const REDIS_NULL_VALUE = "null";
